import { LimitToRangeDirective } from './limit-to-range.directive';

describe('LimitToRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitToRangeDirective();
    expect(directive).toBeTruthy();
  });
});
